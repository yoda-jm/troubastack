# Copy to deploy/.env (gitignored) and set your domain. This is the ONLY value the
# deployment needs. The A/AAAA record for this domain must point at the host BEFORE
# `docker compose up -d`, so Caddy can complete the Let's Encrypt HTTP-01 challenge.
DOMAIN=troubastack.example.org
