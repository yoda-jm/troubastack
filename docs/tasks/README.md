# Task pack — prioritized, agent-executable work items

Each `Txx-*.md` file in this directory is a **self-contained task** written so a competent
coding agent (or human) can execute it without reading the whole repo history. Files are
numbered **in the order they were filed**, not in priority order — priority lives in each spec's
own header and, authoritatively, in the review gate (`docs/handoff/reviews.md`).

`docs/ARCHITECTURE.md` now tags each invariant **✅ enforced today** or **🎯 target**; this
queue is what closes the 🎯 gaps (e.g. T09 for I1's codegen debt, I8's parity test).

## Ground rules (apply to every task)

1. **Read `docs/ARCHITECTURE.md` first.** The numbered invariants (I1…I15) are normative.
   If your change would violate one, stop and flag it instead.
2. **One task = one branch = one PR.** Branch from `main`, name it `task/Txx-short-name`.
3. **Never edit** `core/internal/webassets/dist/` (build artifact, regenerated by `make embed`)
   or anything under a `gen/` directory.
4. **Verification commands** (run from the repo root):
   - `make test` — Go tests (engine, stores, HTTP API). Must stay green.
   - TS typecheck — note the `web/` workspace is nominal: there is **no root lockfile**;
     studio installs standalone (`make setup` runs `npm install --no-workspaces` in
     `web/studio`) and ink resolves from source via Vite alias + tsconfig `paths`.
     So install per-package, then use studio's tsc for all three:
     ```
     make setup                      # once; installs web/studio + Playwright
     cd web/ink && npm ci --no-workspaces   # ink lockfile; --no-workspaces or npm reparents to the (absent) root lockfile
     cd web && TSC=studio/node_modules/.bin/tsc; \
       $TSC --noEmit -p ink && $TSC -b studio && $TSC --noEmit -p bake
     ```
     (studio's tsconfig.json is a SOLUTION file — `-p studio` compiles nothing;
     use `-b studio` so tsc follows the app + node references.)
   - `make e2e` — Playwright end-to-end suite (needs `make setup` once). Must stay green
     for any task touching `web/studio`.
   - `make demo` — builds everything and serves the seeded app at `http://localhost:8080`
     (demo users: `marie` / `leo` / `sasha` / `maestro` / `flora` / `cory`, password `demo`).
     Use this to eyeball UI changes.
5. **E2E selectors:** the Playwright specs in `web/studio/e2e/` select by `data-testid`.
   When you change markup, keep existing `data-testid` attributes attached to the
   equivalent element, and update specs only when a flow genuinely changed.
6. Each task lists its own **acceptance criteria**. All of them must pass before the task
   is done. If a criterion is impossible as written, report why rather than approximating.
   **When a change is observable in the UI (a filename, label, layout, or `data-testid`), the
   acceptance criteria MUST name the e2e suite (`make e2e`) explicitly** — a user-visible change
   gated only by `make test` is exactly how a stale e2e assertion rode `main` red unnoticed for the
   whole T72→T79 window (T81, Part C).

## Queue state

**Do not enumerate landed work here.** This section used to be a hand-maintained list of everything
that had shipped; it was last re-synced **2026-07-18** and by 2026-08-23 it was a month behind — it
still listed P201 as "open, unblocked" (landed), described P205 as the newest arc (the T50–T94 and
A26–A41 arcs came after it), and named a `-mine` demo bridge that no longer exists. Both lanes' handoff
docs pointed here as authoritative, so both were navigating by a map of a month ago.

A list of what has landed is data git already holds, and a copy of it will always rot. So this section
now records **only what git cannot tell you**, and tells you how to derive the rest.

### Deriving current state (do this, don't trust a list)

- **What landed, and why it was accepted:** `docs/handoff/reviews.md` — the review-gate log, newest at
  the bottom. Every task has a submission and a verdict; a landed task's verdict ends with "LAND IT"
  and its code commits carry an `Approved:` trailer.
- **What is on main right now:** `git log origin/main --oneline` and, for one task,
  `git log origin/main --grep='(T94)'`.
- **What is queued for a lane:** the most recent Fable entry in `reviews.md` naming that lane's queue.
  If a handoff doc and `reviews.md` disagree, **`reviews.md` and main win** — and fix the handoff.

### Blocked on the human (git cannot know these)

- **One attended device session** (tablet on adb) owes: **A37** — tune the four `pageColorFilter()`
  matrices at *stage brightness* (they are still the spec's untuned formulas) and shoot all four
  schemes; **A35** — the 3/4 and 6/8 beat shots, where the three tiers are visible.
  **A39's debt is discharged** — its **Available** and **InFlight** rows were finally seen by a human
  during A42①'s device pass (`"Downloading 662 KB / 819 KB"`, rev 7→8 and 8→9 offer detection).
  **:8080 is VLL's preview — never use it for device work.**
- **A33's remaining drills** — the stage-worthiness sheet is live at
  `docs/handoff/STAGE-WORTHINESS.md`. Drill 2 ran and produced **A46**; drill 3's predicted finding was
  **disproved**. Drills **1, 4, 5, 6** are open and each needs something a lane cannot conjure: drill 1
  needs **USB adb** (airplane mode kills a wireless harness's own connection), 4 needs a bundle
  generator, 5 an isolated rig, 6 a storage-quota shim — **never fill the real device**.
- **A07** — tablet stylus spike.
- **IOS03 impl** — blocked on a Mac + Apple credentials (§11 of the mobile handoff is a runbook, not a
  buildable task).
- **T81 Part B** — VLL-owned. A lane must not close it by claiming it.
- Credential rotation for the git remote.

### Open, and worth knowing

- **T24** (converge `cmd/mkcharts` onto `internal/chartpdf`) — **superseded by T95**, which re-examined
  it with evidence and **ruled against a single renderer**: retiring mkcharts would force the chart
  dialect to grow tab, chord-box and drum grammars it has no business carrying. T95 then converged the
  one file that genuinely could (amazing-grace) and **closed**. Read T95's §2 table and its three
  correction notes before re-opening this line of thinking.

### Closed without landing (decisions, not omissions)

- **T14** — panelize measured ~10px and was reverted; superseded by T17.
- **T17** — single-row toolbar / style disclosure; superseded by **T27** (canvas-first editor). Its
  zero-shift-e2e-first requirement carried into T27.
- **P203** — re-affirm the mirrors (closed, not adopted).
- **P202** — closed; the safe slice landed, the rest deferred to **P204**.

## Index — list the files, don't read a list

There is no hand-maintained index here any more, for the same reason "Queue state" no longer enumerates
landed work: a copy of what the filesystem already knows will always rot. This section used to table
T01–T69 with inline "— landed" notes while the tree had reached **T105, A46, B15** — roughly sixty
tasks missing, and status annotations frozen at whatever day someone last touched them.

```
ls docs/tasks/                          # every task, by number
git log origin/main --grep='(T94)'      # did one task land, and in what
git show origin/main:docs/handoff/reviews.md | tail -200   # the gate: submissions + verdicts
```

**Numbers are filing order, not priority.** A task's priority is in its own header; what a lane should
do *next* is the most recent Fable entry in `reviews.md`. If a doc and `reviews.md` disagree,
**`reviews.md` and main win.**

For **what already shipped and the commit that did it**, see the [Archive](#archive--landed-tasks-and-the-commits-that-did-it)
at the bottom — a SHA is permanent fact, so that table records the one thing about a finished task
that cannot rot.

What follows is only the part git cannot tell you: how tasks constrain each other.

### Cross-task constraints

**None are currently binding.** The paragraph that used to live here sequenced T01/T02/T05/T06/T10/T13/
T14/T15 — every one of which has landed, and the CI e2e job it warned about (`continue-on-error` until
T13) now hard-gates. Its one durable fact, that T14's "panelize" approach measured ~10 px and was
reverted, is recorded under *Closed without landing* above; it was duplicated here.

Constraints do recur, so state them **in the task that has one** rather than here, where they outlive
their cause. The recurring shapes worth naming:

- **Two tasks editing the same file** should not run in parallel (the song editor and the bake pipeline
  have both produced this).
- **A task whose only safety net is a full `make e2e`** should run attended on an unloaded machine —
  three-way contention has produced spurious reds that were all green in isolation.
- **A task that renames or moves a UI affordance** owes the e2e migration in its own estimate: check
  `grep -rn 'getByTestId("<id>")' web/studio/e2e` before sizing it. T104 is the current example — seven
  call sites reach the chart editor through one menu item's testid.

## App track (A-series) — the Kotlin/Compose mobile app

`app/` is shared Kotlin (Compose Multiplatform) behind three `expect/actual` seams.
The A-series turned it into a working Android app (all landed), and the IOS-track
filled the iOS actuals (InkOverlay still `TODO`, A07-gated). The design remains
**TroubaStage-first**: the presenter is the product's stage-time face and its contract
is *resilient and read-only* — it must never crash mid-performance, and its only
user-changeable state is reading behavior (navigation, display fit, layer visibility),
never writes. It is also **offline and login-free** (I12): no account, session, or
unlock stands between app launch and performing a locally-stored bake; the viewer's
role (for layer default visibility) is a local preference, not an identity. Auth exists
only in the Studio webview (A06) and the future downloader. See A04 for the full
contract.

The A-series files are `A01`…`A46` — `ls docs/tasks/A*.md`. Ordering that still binds:

A-track ordering: strictly A01 → A02 → A03 → A04 → A05. A06 needs only A01 and may run
in parallel with A02–A05 (different files). A07 is **blocked** until T06 lands *and* the
stylus spike on the real tablet says the optimized web path isn't enough — if it is
enough, A07 closes unbuilt.

The downloader/update-offers work (`distribution/Updates.kt`, invariant I13 — explicit
updates, transient auto-update, freeze/lock) is **B03's app half** — unblocked now that
the bake pipeline is real and the server exposes the `AvailableConcert` manifest. iOS
is no longer "later": IOS01–IOS04 are landed (Storage/WebViewHost actuals, the Xcode
`iosApp` shell, simulator CI proof, Stage keep-awake); only InkOverlay stays `TODO`
(A07-gated).

A-track verification commands: `cd app && ./gradlew :shared:check :androidApp:assembleDebug`
(after A01), plus each task's own criteria. Keep invariant **I15** sacred: platform code
lives only in the six seam actual files and the thin `androidApp` module — if you're
about to add a platform-specific file anywhere else, stop and re-read
`docs/design/07-boundaries-and-no-duplication.md`.

## B-track — bake · publish · distribute (the last core subsystem)

Closes the product loop: *compose → bake → distribute → perform*. **B01 and B02 are
landed** (bake → download works, live-verified at review); B03's server slice is landed
and its app half is the open piece — after it, no manual file transfer is needed at all.
B05 retires the hand-baked `docs/demo` bundle now that the real pipeline exists.

The B-series files are `B01`…`B15` (plus `OPS01`/`OPS02`) — `ls docs/tasks/B*.md`.

Two design decisions are resolved *inside* the specs (don't relitigate without cause):
setlist overrides ride as bundle-manifest metadata (B02); app auth reuses session login
with the secrets-hardening made mandatory (B03).

## iOS track — honest about the Mac problem

No Mac, no Apple credentials in this loop. IOS01 is Linux-verifiable (klib
cross-compilation); IOS02 proves the app on **GitHub's macOS runners + simulator, which
need no Apple account** (manual/weekly trigger — macOS minutes are 10× on private
repos); IOS03 is a decision stub (devices/App Store need a Mac + Apple ID).

| # | Task | Area | Size |
|---|---|---|---|
| IOS01 | [Enable iOS targets + Storage/WebView actuals](IOS01-ios-targets-and-actuals.md) | app | M/L |
| IOS02 | [iosApp + simulator proof in CI](IOS02-simulator-ci.md) | app + CI | M/L |
| IOS03 | [Devices / App Store — **BLOCKED** decision stub](IOS03-ios-device-and-store.md) | — | — |
| IOS04 | [Stage host: keep the screen awake while performing](IOS04-stage-keep-awake.md) | app | XS |

## Phase 2 — opt-in modes & debt retirement

| # | Task | Area | Size |
|---|---|---|---|
| P201 | [Rehearsal live mode: autobake + transient auto-update](P201-rehearsal-live-mode.md) | core+studio+app | L |
| P202 | [Real GC: cross-layer reachability + retention](P202-real-gc.md) — safe slice landed, rest re-scoped → P204 | core | M/L |
| P203 | [Adopt proto codegen (staged decision)](P203-codegen-adoption.md) — DECIDED: re-affirm mirrors (closed) | all | L |
| P204 | [Annotation-history compaction (P202's deferred half)](P204-history-compaction.md) — DEFERRED until real disk pressure | core | M/L |

P201 depends on B03. P202 benefits from B02 (bake source-revision roots). P203 starts
with a cheap decision stage — do that anytime, execute stages only after review.

## Archive — landed tasks and the commits that did it

Every task below has at least one implementing commit on `main`. **A commit SHA is permanent fact**,
which is why this table can exist where a "status: landed" column could not — the thing it records
cannot go stale. Generated by matching each task id against commit subjects outside `docs/handoff/`
and `docs/tasks/`; the SHA shown is the **earliest** implementing commit, `+N` counts the rest.

To see one task's full story: `git log origin/main --oneline --grep='T94'`, then its verdict in
`docs/handoff/reviews.md`.

| # | What it did | First commit |
|---|---|---|
| A01 | [Wire the Gradle build](A01-wire-gradle-build.md) | `db9bf8e` |
| A02 | [Concert-bundle domain model + resilient loader](A02-bundle-model-and-loader.md) | `a3aff02` |
| A03 | [Fixture bundle generator + committed demo bundle](A03-fixture-bundle-generator.md) | `5b2f755` |
| A04 | [TroubaStage presenter: resilient read-only compositor + pager](A04-troubastage-presenter.md) | `1e18aa4` |
| A05 | [Android Storage actual + `.tstage` bundle import](A05-android-storage-and-import.md) | `21a991d` |
| A06 | [Android WebViewHost actual: TroubaStudio in the app](A06-android-webview-studio-host.md) | `6927793` |
| A08 | [Stage shows the setlist metadata](A08-stage-setlist-metadata.md) | `07e1e5d` +1 |
| A09 | [Stage: hardware page turns](A09-stage-hardware-page-turn.md) | `02808fb` +1 |
| A10 | [Stage night mode: don't blind the performer in a dark venue](A10-stage-night-mode.md) | `a34d4fc` +1 |
| A11 | [Stage: optional visual count-in](A11-stage-visual-count-in.md) | `b537d5f` +1 |
| A12 | [Stage: facing pages (two-up) in landscape](A12-stage-facing-pages.md) | `57b2c9b` +2 |
| A13 | [Stage: volume keys must turn by spread in two-up](A13-stage-volume-spread-turn.md) | `66e872f` |
| A14 | [Stage: continuous-scroll reading mode](A14-stage-continuous-scroll.md) | `06bfb4d` |
| A15 | [Stage: song-jump navigation drawer](A15-stage-song-drawer.md) | `6619496` +1 |
| A27 | [App landing page: products + identity, not the bake list](A27-app-landing-page.md) | `24a1a83` +1 |
| A34 | [Stage: make the visual beat actually visible](A34-visual-beat-rebuild.md) | `9a16afb` +4 |
| A35 | [Stage: beat the song's metre, not an assumed 4/4](A35-stage-meter-beat.md) | `9d2beae` +1 |
| A36 | [Give the app the website's colours instead of Material's default purple](A36-app-theme-parity.md) | `7fe44e6` +3 |
| A37 | [Stage reading colour schemes](A37-stage-reading-color-schemes.md) | `39e47a6` |
| A38 | [Home's connection status becomes a connection *control*](A38-home-connection-control.md) | `49299e0` +2 |
| A39 | [One tap from Home to get the current concert onto the device](A39-one-tap-concert-update.md) | `d73d982` +2 |
| A40 | [The beat is a count-in by default; ∞ is opt-in](A40-count-in-not-infinite-by-default.md) | `ed0b3da` |
| A41 | [Sign in should remember who you are](A41-remember-the-username.md) | `0c49ad4` |
| A42 | [Real progress in the app: a determinate update bar, and one-tap bake from Home](A42-app-operation-progress.md) | `50f5556` +2 |
| A43 | [The Home landing must not claim "Up to date" when it doesn't know, or when you…](A43-home-landing-tells-the-truth.md) | `07d9423` |
| A44 | [The update-status transition belongs in shared, where it can be tested](A44-update-status-transitions-are-testable.md) | `f3a6e6b` |
| A45 | [Home's connection controls move to a top-right account trigger](A45-home-account-trigger.md) | `e9a87be` |
| B01 | [web/bake: the headless overlay renderer](B01-bake-worker.md) | `00a7da5` |
| B02 | [Core bake orchestration + Studio "Bake" button](B02-bake-orchestration.md) | `869d900` +2 |
| B03 | [Distribution: in-app downloads, offers, freeze](B03-distribution-and-updates.md) | `97052eb` +4 |
| B04 | [Bake robustness: atomic rev publication + concurrency guard](B04-bake-write-atomicity.md) | `d085b63` |
| B05 | [Regenerate `docs/demo` via the real bake pipeline](B05-regenerate-demo-bundle.md) | `5602ff5` +3 |
| B06 | [LAN auto-discovery: find the band's server without typing an IP](B06-lan-discovery.md) | `ac0066e` +1 |
| B07 | [Per-member bake: "Leo sees his tab on stage"](B07-per-member-bake.md) | `2a53bfe` +2 |
| B08 | [Bake rev-claim race: publish `rename` can fail against a concurrently-publishe…](B08-bake-revclaim-race.md) | `86f1edd` |
| B09 | [Bake re-claim path can orphan/mismatch a published `<rev>.tstage`](B09-bake-reclaim-tstage.md) | `902ea34` |
| B10 | [Seed a real text-chart lyrics file into the demo](B10-demo-text-chart.md) | `0c760ed` +1 |
| B11 | [Demo per-file annotations: show each part carrying its OWN notes](B11-demo-per-file-annotations.md) | `c76a0cf` |
| B13 | [Demo annotation showcase v2: every tool, real help, anchored placement](B13-demo-annotation-showcase-v2.md) | `42c4ed9` |
| B14 | [Seed your OWN band from a local folder](B14-local-band-folders.md) | `9cbc177` |
| B15 | [Seed **all** a song's chart parts from its folder](B15-seed-multiple-chart-parts.md) | `0a82036` |
| CFG01 | [A configuration file for troubacore](CFG01-configuration-file.md) | `78f968a` |
| DEMO-VID | [TroubaShare walkthrough video](DEMO-VID-walkthrough-video-plan.md) | `b7bc748` +11 |
| IOS01 | [Enable iOS targets + fill the Storage/WebView actuals](IOS01-ios-targets-and-actuals.md) | `8e53e42` |
| IOS02 | [iosApp + simulator proof on GitHub's macOS runners](IOS02-simulator-ci.md) | `e786418` +6 |
| IOS04 | [iOS Stage host: keep the screen awake during a performance](IOS04-stage-keep-awake.md) | `5946874` |
| OPS01 | [Production serving: TLS, service, backup, release APK](OPS01-production-serving.md) | `3662468` |
| OPS02 | [Docker image embeds the ready apps + a download link in Studio](OPS02-embed-apps-in-image.md) | `8b1e006` +1 |
| P201 | [Rehearsal live mode: autobake + transient auto-update](P201-rehearsal-live-mode.md) | `eaa393f` +8 |
| P202 | [Real GC: cross-layer reachability + retention tiers](P202-real-gc.md) | `5ceba9f` |
| P205 | [Band-wide bundle: view-time identity + bake-time defaults](P205-band-wide-bundle.md) | `0c1d836` +12 |
| T01 | [Fix web workspace typecheck breakage](T01-fix-web-workspace-typecheck.md) | `4ebdb3e` +1 |
| T02 | [GitHub Actions CI](T02-github-actions-ci.md) | `5ed1fe5` +1 |
| T03 | [UI: one accent color + dark-mode fixes](T03-ui-design-tokens-dark-mode.md) | `d33b3b7` |
| T04 | [UI: simplify pages](T04-ui-simplify-pages.md) | `d78e54c` |
| T05 | [UI: compress the song-editor chrome](T05-ui-editor-chrome.md) | `e045613` |
| T06 | [Low-latency wet-ink drawing path](T06-wet-ink-performance.md) | `9fac1f4` |
| T07 | [Annotation-type descriptor registry](T07-annotation-type-registry.md) | `246a5ec` |
| T08 | [Close the REST-import authorization gap](T08-rest-import-authorization.md) | `d901595` |
| T09 | [Proto codegen: generated mirrors, drift-guarded](T09-proto-reconciliation.md) | `704dc0a` +8 |
| T10 | [Split SongEditor.tsx](T10-split-song-editor.md) | `01d98d7` |
| T11 | [Remove dead wiring in core](T11-core-dead-wiring.md) | `7199605` |
| T12 | [Make ARCHITECTURE.md enforcement claims honest](T12-docs-honesty.md) | `4fb7739` |
| T13 | [e2e: RO-vs-RW footprint shifts ~27px in CI headless](T13-e2e-rorw-footprint-ci.md) | `66fcb19` |
| T15 | [Split Viewer.tsx into pdf/overlay/sync hooks](T15-viewer-hooks-split.md) | `4f26f02` +2 |
| T16 | [Seed PDFs render em-dashes as mojibake](T16-seed-pdf-encoding.md) | `96ae26c` |
| T18 | [De-dup the Go `ConcertBundle` mirrors](T18-dedup-bundle-mirrors.md) | `5602ff5` +1 |
| T19 | [Text charts: write formatted song documents in Studio, bake them like PDFs](T19-text-charts.md) | `9058aa9` +2 |
| T20 | [Duplicate a setlist](T20-setlist-duplicate.md) | `8257d54` |
| T21 | [Password reset](T21-password-reset.md) | `473557d` +1 |
| T22 | [Deterministic list ordering](T22-deterministic-listings.md) | `1c7a5e7` +1 |
| T23 | [Encore / bench songs: baked and jumpable, outside the running order](T23-encore-bench-songs.md) | `59ba8e2` +2 |
| T25 | [Chart editor: rendered PDF preview pane](T25-chart-editor-preview.md) | `a3a93da` |
| T26 | [Carry song titles in the baked bundle](T26-bundle-song-titles.md) | `a789813` +2 |
| T27 | [Canvas-first editor](T27-canvas-first-editor.md) | `b3179b6` +12 |
| T28 | [Drawing on a hidden layer silently swallows the annotation](T28-hidden-layer-draw-swallow.md) | `81b7594` |
| T29 | [Embed the git version in the binary: REST endpoint + UI info modal](T29-build-version-visibility.md) | `cb92ec9` |
| T30 | [No silent ink: every can't-commit state is visible](T30-no-silent-ink.md) | `b026bdb` |
| T31 | [Bake ignores per-object z-order: baked overlays can stack differently than stu…](T31-bake-zorder-parity.md) | `514e8bd` |
| T32 | [`crypto.randomUUID` missing on plain-HTTP origins: all creation silently throws](T32-insecure-context-uuid.md) | `1cb1286` |
| T33 | [Thin the contextual style pill to the main bar's height](T33-thin-ctx-bar.md) | `0e4381a` +2 |
| T34 | [Touch editor stuck in navigation after a two-finger gesture](T34-stuck-nav-stale-pointer.md) | `c2d75a9` |
| T35 | [Slow freehand at reduced opacity shows dark bands](T35-wet-alpha-stacking.md) | `21698ac` |
| T36 | [Files management + Delete song reachable in the editor](T36-details-panel-files-delete.md) | `fa08727` |
| T37 | [New text chart from lyrics: best-effort URL fetch + paste fallback](T37-lyrics-paste-import.md) | `5966b78` |
| T38 | [Auto-label verses/choruses on lyrics import](T38-lyrics-auto-sections.md) | `2c213d6` |
| T39 | [Rich pseudo-md source editor for the chart dialect](T39-chart-live-preview.md) | `febb592` |
| T43 | [One-finger marquee-select in Select mode on touch](T43-touch-marquee-select.md) | `97d6d86` |
| T44 | [Total canvas-area budget clamp](T44-canvas-total-area-clamp.md) | `e6a45cf` |
| T46 | [Studio embedded mode](T46-studio-embedded-mode.md) | `2175b31` |
| T50 | [Personal song cues: icons + colors per song per member](T50-song-cues.md) | `1ebc785` +5 |
| T51 | [Icon stamp annotation tool](T51-icon-stamp-tool.md) | `6577031` +3 |
| T52 | [Setlist reorder motion: FLIP animation + drag-hint flicker fix](T52-setlist-reorder-motion.md) | `1dcfe34` +1 |
| T54 | [Song Details panel: tabs by audience](T54-details-tabs.md) | `1c79f77` |
| T55 | [Zone-at-draw-time chip: the "Drawing on:" indicator declares its audience](T55-draw-zone-chip.md) | `0492bf2` |
| T56 | [Band/Mine vocabulary sweep](T56-audience-sweep.md) | `d12d1ae` |
| T57 | [Print a baked concert to PDF](T57-concert-pdf-export.md) | `0ebb346` +1 |
| T58 | [Topbar account dropdown](T58-account-dropdown.md) | `2bcbc0f` |
| T59 | [Editor scroll overscan: free the first page's top + last page's bottom from th…](T59-editor-scroll-overscan.md) | `4db2cbb` +2 |
| T60 | [Chord transposition for text charts](T60-chord-transposition.md) | `050ce8c` +8 |
| T61 | [Setlist item → song page hyperlink](T61-setlist-song-links.md) | `5df594f` |
| T62 | [Band export / import](T62-band-export-import.md) | `e4d1fe6` +1 |
| T63 | [Consent-required import (invite-on-import) + integrity/DoS hardening — **SECUR…](T63-invite-on-import.md) | `37d6428` |
| T64 | [Chord-transposition correctness fixes](T64-transpose-correctness-fixes.md) | `eeaf313` |
| T65 | [Editor: move/pan tool + dashed marquee + scrollable tool row](T65-editor-move-tool-marquee-overflow.md) | `10eda49` |
| T66 | [Move-first/default + select-as-dashed-rect icon + compact mobile editor chrome](T66-editor-move-default-select-icon-mobile-chrome.md) | `7a70f48` +1 |
| T67 | ["Save chart" shows a stale render](T67-chart-save-stale-render.md) | `45d2ca2` |
| T68 | [Persist the open file in the URL](T68-open-file-in-url.md) | `ab4b0b2` |
| T69 | [Self-heal a generated chart whose rendered PDF blob is missing](T69-missing-blob-selfheal-chart.md) | `a022e89` |
| T70 | [Text-chart subtitle (artist) in the rendered header — made explicit, not guess…](T70-chart-subtitle-header.md) | `7a886c2` |
| T71 | [Studio: "search by artist/title" in the lyrics dialog](T71-lyrics-search-by-song.md) | `bf20958` |
| T72 | [A text chart's pool name is derived from the title and clobbered on every edit](T72-text-chart-filename-clobber.md) | `93e147c` |
| T73 | [chartpdf: chord rows with a trailing `(…)` note, accented section names, tight…](T73-chartpdf-chord-annotations-and-section-accents.md) | `44248b0` |
| T74 | [Per-chart font size, as a header-block directive](T74-chart-font-size-directive.md) | `2deaf27` |
| T75 | [Chart compaction: reclaim wasted page space at the SAME font size](T75-chart-compaction.md) | `85f7c4c` |
| T76 | [Auto-fit: pick the largest size that keeps a chart on one page](T76-chart-auto-fit.md) | `127519f` |
| T77 | [A meaningful page break: an explicit `{new_page}` marker + no orphaned sections](T77-chart-page-break.md) | `b6a02a1` |
| T78 | [The Files section as a sortable list with a per-row menu](T78-files-list-sortable-rows.md) | `b98e5b4` |
| T79 | [One way to add a file: three entries, one shape — plus good default names](T79-add-file-flow-and-names.md) | `518c91b` |
| T80 | [One add-file dialog shell: unify the three entries' interaction model](T80-add-file-dialog-shell.md) | `12b0e12` |
| T81 | [Make the e2e core port configurable, and prove the e2e gate actually gates `ma…](T81-e2e-port-and-ci-gating.md) | `9a3d760` |
| T82 | ["My files": a checkbox must never move or resize the row it is on](T82-my-files-stable-list.md) | `4dd35c8` +1 |
| T83 | [Delete a layer: tiered confirmation, and a cascade that doesn't punch a hole i…](T83-delete-layer-confirmation.md) | `15d84f9` |
| T84 | [Stroke width: usable steps, and headroom above chart-text size](T84-stroke-width-range.md) | `4439e0c` |
| T85 | [Studio: the same visual beat, from the same contract as the app](T85-studio-visual-beat.md) | `f27b755` +2 |
| T86 | [Give a song its metre, and make the beat count bars instead of guessing 4/4](T86-song-meter.md) | `09cb3fe` +2 |
| T87 | [The per-file "⋯" menu is clipped away by its own container](T87-row-menu-clipped.md) | `66b4b2d` |
| T88 | [The icon palette should hug the page, not the far viewport edge](T88-icon-palette-hugs-page.md) | `fe10c11` |
| T89 | [The Details panel has no way out on a phone](T89-details-panel-no-way-out.md) | `6874e9a` |
| T90 | [The text tool traps you: every tap is another modal prompt](T90-text-tool-traps-you.md) | `e4219c0` |
| T91 | [Replace the studio's blocking browser dialogs](T91-no-blocking-browser-dialogs.md) | `12ba443` |
| T92 | [Pin the metre parser with shared vectors, before a third runtime copies it](T92-meter-groups-vectors.md) | `a8ad0e5` |
| T93 | [de-flake `fourFileRows`](T93-fourfilerows-flake.md) | `a27a252` |
| T94 | [Layers, Notes and Details: two honest classes, one dismissal contract](T94-panels-one-contract.md) | `6088c60` |
| T95 | [The two PDF renderers: gap analysis, and converge what can converge](T95-two-renderers-gap-and-convergence.md) | `b2a22ca` +3 |
| T96 | [Bake progress: "song 3 of 11", observable while it runs](T96-bake-progress.md) | `6f4e05c` |
| T97 | [A bake spawns two external processes PER SONG, even with nothing to draw](T97-bake-slow-per-song-spawns.md) | `138e46f` +1 |
| T98 | [Batch the overlay worker across songs: one node spawn per bake, not N](T98-batch-overlay-cli.md) | `4e31915` |
| T99 | [Show the bake progress in the dialog](T99-bake-progress-ui.md) | `54e00f7` |
| T100 | [A local band folder can define its concerts](T100-local-band-setlists.md) | `ae14d4f` |
| T101 | [BUG: the text-annotation prompt doesn't appear on mobile](T101-text-prompt-missing-on-mobile.md) | `fd40c3c` |
| T102 | [A failed bake tells the user what went wrong, not a stack trace](T102-bake-errors-for-humans.md) | `7a10e7a` |
| T103 | [Baking must be a kick, not a held-open socket](T103-bake-is-a-kick-not-a-wait.md) | `0cf5cef` +1 |

**Not in the table (13), and why** — no implementing commit exists for:
**A07**, **A32**, **A33**, **A46**, **IOS03**, **P203**, **P204**, **T14**, **T17**, **T24**, **T45**, **T104**, **T105**. Those are the blocked (A07 stylus spike, IOS03 Mac),
the open (A32, A33, A46, T104, T105), the deliberately deferred (T45, P204 — their own headers say so),
and the closed-without-landing decisions (T14, T17, T24, P203) described earlier in this file.
