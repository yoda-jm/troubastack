# Session summary — 2026-07-13 → 2026-07-19 (architect/reviewer)

Previous: SUMMARY-2026-07-12.md. Everything below is landed + CI-green + reviewed
(verdicts in reviews.md) unless marked otherwise.

## The arcs

**Device-QA sweep (VLL's Redmi/Pixel runs)** — T44 black pages (root cause:
`desynchronized:true` overlay compositing; budget+recovery+desync-gate; T45 stays
deferred with evidence), T47 header/popover, T48 shell-viewport (`svh`), T49 files
header, rotation + WebView-login fixes (origin-bound session), A16 Edit app bar.

**The N-series (N1–N10, all from VLL device feedback)** — any-tap-toggles-chrome
(N3 reversed A2's edge-tap on evidence), song-boundary cue (N1), per-song scroll
(N2), page-turn slide (N4) + quality prefetch/shared-axis (N9), chrome contrast
(N5), song-aligned spreads (N6, + A22 true-no-op fix), edge flash (N7), scroll
swipe-crosses-songs (N8), layers list fixes (N10 → A24/A25/T53/B13: per-song,
no hashes, real baked names).

**Cues & stamps (VLL features)** — T50 personal song cues end-to-end (editor →
my-cues API → per-member bake → app badges + entry flash; A23 squares + "+" +
first-song polish), T51 icon-stamp tool (page-space glyphs, zero app work), the
18-glyph shared contract (one generator → glyphs.json + CueGlyphData.kt, both
CI-drift-guarded).

**Scheme A (global-vs-personal legibility, VLL picked A)** — 09-design doc, T54
audience-tabbed Details, T55 draw-time zone tag, T56 web sweep, A26 app sweep.
One Band 👥 / Mine 👤 vocabulary everywhere.

**P205 band-wide bundle (VLL's model, program complete on main)** — spec + stages:
proto (roster/owner/default_on/member_cues), explicit bake dialog, band-wide THE
bake, app identity (auto-match skips the picker per VLL's strong-default; free
unverified picker per the declutter-not-privacy ruling), owner filtering, A29
shared view-resolution vectors (print == screen TESTED cross-lane), A30 acceptance
kit. T57 concert-PDF rides the same rule. VLL's "(a) land both close together"
superseded my (b) bridge — reconciled; bridge cleanup is web-core's open tail.

**Other** — A27 Home landing page (branded tiles, clean identity line), T52 FLIP
reorder + flicker fix, editor-zorder flake properly killed (reproduced under load,
budget fix), USER-JOURNEY/queue doc syncs, I1 amended (glyph contract under its
umbrella), demo bundles regenerated + parse-verified repeatedly.

## Open

- Web-core: `-mine` bridge deletion (unblocked); later `scope=mine` retirement
  (one overlap release).
- VLL attended: **P205 acceptance (ACCEPTANCE-P205.md, ~10 min)**, P201 two-device
  rehearsal, OPS01, PAT rotation (re-flagged — token echoed once), LICENSE, A07,
  T24, B07 shots.
- Deferred-by-ruling: T45, T52-p2/N4(b), P204, sensitive-layer password, iOS track.

## Process notes (owned on the record)

- My N6 "no-op by construction" claim was wrong (A22 fixed it; state-level-no-op
  lesson logged). A24's CI red = same-SHA flake (analyzed, spec hardened later).
  A stale consistency flag corrected (verify-against-current-main memory). A16
  landed without its trailer once (fix-forward; lanes have cited since). VLL
  rulings recorded verbatim where they superseded mine (T38/T39 pattern held).
