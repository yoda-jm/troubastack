# Session summary — 2026-07-22 (architect/reviewer)

Previous: SUMMARY-2026-07-19.md. Everything below is landed + CI-green + reviewed
(verdicts in reviews.md) unless marked otherwise.

## The arcs

**T09 codegen CLOSED (all 4 stages of the day: 2a/2b/3/4)** — the four-language
mirror set is fully generated (Go, TS-studio, TS-ink, Kotlin `BundleModel.kt`), five
CI drift-guards, and `buf breaking` firing on the real landing path (my CONDITIONAL
caught the push-vs-PR trigger gap; red-proofed locally with a field renumber → exit
100, then live-fired green on the landing). I1 flipped ✅ enforced in ARCHITECTURE.md
— the 07-08 "most expensive bug class" debt is paid.

**T58/T59 (VLL device feedback)** — T59 editor overscan (root cause was a DEAD
chrome-reserve clobbered by the density pass, not missing overscan; density reversal
signed off on VLL's direct word) and T58 topbar account menu (dot + no-shift popover).
**A11 routing lesson:** the same VLL quotes got relayed to mobile as a Stage count-in
bug AND read by web as topbar feedback — VLL confirmed studio; my HIGH-rank mobile
steer retracted as phantom. Standing relay rule: pin the SURFACE in every feedback
relay (memory + reviews.md).

**T60 chord transposition — spec'd 07-21, fully retired 07-23 00:30** (VLL work-order
autonomy, post-hoc reviews all mine, every landing CI-green):
- A engine (`050ce8c`+`babb093`): chartpdf ParseKey/Transpose/TransposeSemitones;
  line-count+geometry invariant (annotations stay anchored) + chords-over-words
  anchoring with glued-then-realign; collision policy signed off.
- B editor (`19a9509` endpoint + `9abf83e` UI): :transpose (dryRun/persist/409/400,
  optional song-key update), SongDetails "Transpose…" control.
- C setlist bake-time (`eb5e062` server + `14a43dc` UI): TransposeChords item flag,
  `app.TransposeEligible` single-predicate (bake decision = warning = preview =
  tooltip), degraded bake warns-never-fails, item chart-preview, greyed checkbox.
- Riders (`f056361`): dirty-editor guard; durable bake-warnings tests (poppler
  adaptation accepted — real-bake httpapi test + mocked-bake banner e2e).
- One CI red: the lane's own new spec raced (add-item reload resets the select);
  root-caused from the CI log, lane fixed with the per-add toHaveCount idiom.

**T61** (`5df594f`): setlist item title → song editor via real `Link`; grip-only
reorder survives; row look preserved (pixel-verified rest+hover).

**T24 RULED (was routed to me):** PARKED, rider-only — no `NewDoc` partial (API
surface for zero output change), full (b)-reconcile rides the next deliberate
attended demo-regen. Premise corrections recorded (chordLine diverged in T19;
option (a) infeasible for tab/blank-staff/meta-header).

## State

Web-core is DRY pending VLL: P201 go · OPS01 attended HTTPS · A07 stylus session ·
PAT rotation · `scope=mine` retirement (needs VLL's overlap-release call). Mobile:
A32/A33 per its own queue. Demo on :8080 carries transposition + linking.

## Process notes

- Two landings without `Approved:` trailers (`babb093`, `8b86049`) — flagged; lane
  resumed citing from T61 on.
- CI job-log fetch gotcha saved to memory: the logs endpoint 302s to Azure; strip
  the auth header on the redirect or it 403s.
- Reviews ran from the `../troubastack-review` worktree + throwaway :8092/:5175
  stacks throughout — the primary checkout belongs to the active lane.
