# Agent handoffs

Per-agent handoff notes for the Claude Code agents working this repo. Point a fresh session at the
file for its lane to continue seamlessly — each captures *how that agent works* and *what's done*,
not the code (read the code + `docs/` for that).

- [`mobile-app-agent.md`](mobile-app-agent.md) — the **A-track** (Kotlin/Compose Multiplatform app in `app/`).

(Add a file here per agent/track as they come online, e.g. a T-track handoff.)

These are living docs; keep the "what's done / remaining" sections current as tasks land.
