# Summary — Monday night 2026-07-06 → Tuesday 2026-07-07 (architect/reviewer)

*Picks up exactly where [`SUMMARY-2026-07-04-to-06.md`](SUMMARY-2026-07-04-to-06.md)
ends (that one closes at Monday afternoon's architecture audit + tablet screenshots).
Every claim below is backed by a verdict in [`reviews.md`](reviews.md) and every
landing was independently re-verified — builds/tests re-run fresh, artifacts
dissected, UI checked by pixels, one bake driven live end to end. Commits are
as-landed on `main` (linear, CI-green unless noted).*

## TL;DR

**Vincent's field report got closed and the two feature tracks he asked for got
built.** Since Monday afternoon: the Stage grew a complete reading-ergonomics arc
(metadata strip → hardware turns → night mode → count-in → facing pages → volume-key
fix → song drawer → continuous scroll, **A08–A15 all landed**), the web side shipped
**text charts (T19)**, **deterministic listings (T22)**, **admin password reset (T21)**,
**setlist duplication (T20)**, **per-member bakes (B07)**, a **config file (CFG01)**,
and **encore/bench songs (T23)**. A user-reported "annotations in the wrong place /
random song order" field report was triaged to two causes (stale pre-fix data + a real
ordering bug), both fixed and verified. ~20 task/feature landings across the two lanes,
each held at the gate and re-verified here; every review finding either fixed or filed
as a follow-up (T24/T25/T26/B08). Zero regressions shipped.

---

## Monday night 07-06 (the tail after the weekend summary was written)

- **B06 core slice** (`ac0066e`) — the band server advertises itself on the LAN over
  mDNS (`_troubacore._tcp`, opt-out env, register-failures-never-block-serving); the
  prefill-not-trust security stance held. App-side browse (NsdManager/NWBrowser) routed
  to the mobile lane.
- **A09 hardware page turns** (`02808fb` + evidence) — BT pedals/keyboards + volume keys
  turn pages; volume interception is Stage-scoped (normal everywhere else). Keyevent
  tour verified by screenshot (PAGE_DOWN→DPAD→VOLUME→SPACE lands 3/22).
- **T20 setlist duplicate** (`8257d54`) — deep copy with fresh ids (no bake history, no
  aliasing); member-gated; e2e'd. USER-JOURNEY gap #7 closed.
- **T21 password reset** (`473557d`) — admin/operator mints a single-use, SHA-256-hashed,
  expiring reset link (out-of-band handover, no SMTP by design); consuming it
  invalidates every session. Crypto verified by read, not trusted. Gap #8 closed. A
  follow-up (`d30acc6`) renders the link as a QR (reusing the invite-panel pattern).
- **Seed page-doubling fix** (`a014d75`) — the placeholder-PDF generator's footer sat
  below fpdf's auto-break threshold, so **every page spilled a blank+footer page**
  (12 real → 22). Fixed with `SetAutoPageBreak(false)` (we paginate manually); a
  regression test pins `/Count == requested`. This also explained the "annotations off
  on page 2/3" mystery in the field report.
- **B07 per-member bake** (`2a53bfe`) — "Leo sees his tab on stage": `setlist~user`
  variant bakes with their own file selection, its own rev line, member-gated so no
  cross-member leak (verified the two negatives that matter). USER-JOURNEY's top
  post-loop gap closed.
- **A10 / A11 / A12** (`a34d4fc` / `b537d5f` / `57b2c9b` + a batched evidence run) —
  night mode (draw-time RGB inversion, alpha untouched), visual count-in (tap the tempo
  chip, silent pulse), and facing pages (two-up in landscape FIT_PAGE, spread-aware
  turns). All code-approved, then finalized together on pixel evidence.

## Tuesday 07-07 — field report closed, two feature tracks built

### Field report → fully closed
- **Triaged** (`a4cab51`): reproduced fresh on a post-fix server. The misplaced
  annotations + strange pages were **stale pre-fix doubled-page data** (any instance
  seeded before `a014d75`), not a live bug — proven by a clean fresh bake. The random
  song order was a **real bug**: `SongsOfBand` (and siblings) ranged Go maps.
- **T22 deterministic listings** (`1c7a5e7`) — one ordering helper set applied
  symmetrically across 7 listing methods on both repo backends; ordering asserted in
  endpoint tests. Approved **with one gap** (invites/invite-links still unsorted) →
  **fix-forward** (`3c9ce14`) closed it. Vincent's reshuffling-list bug is dead.
- **Demo bundle regen** (`97a449b`) — the shipped `.tstage` still carried the doubled
  22 pages; regenerated to the true **12 pages**, placement re-verified by compositing
  overlay layers over pixels. README page counts corrected.

### CFG01 — configuration file
- **Decided then built** (`78f968a`): INI via `gopkg.in/ini.v1`; precedence
  **defaults < file < `TROUBA_*` env < flags** (every env var still works — CI/Makefile
  untouched); a single knob table is the source of truth for both loading AND the
  generated `troubacore.example.ini` (byte-equality test so docs can't rot); ADR 0004.
  Verified live: file values apply, env beats file, explicit-missing `--config` exits 1,
  byte-equality holds. The last stray env read (`TROUBA_NO_MDNS`) hoisted to the
  composition root. Reserved commented `[smtp]` forward hook; **no SMTP now** (T21 is
  email-free by design).

### Reading-ergonomics arc — proposal → A13/A14/A15 (mobile lane)
- **Proposal validated** (`032812d`): the mobile lane mined the legacy app; I confirmed
  its §1 defect by reading the code and specced **A13/A14/A15** (+ **T23** to the
  web-core lane).
- **A13** (`66e872f`) — volume keys turn by *spread* in two-up (they were the one input
  still wired turn-by-1). Fixed via a commonMain CompositionLocal so `StageScreen` owns
  the turn; better than the spec's approach (no re-registration churn).
- **A15 song drawer** (`6619496`) — the "Songs" dropdown promoted to a
  `ModalNavigationDrawer` with per-song meta lines + current-song highlight; drawer
  gestures enabled only while open (can't fight the page-turn swipe — a stage-safety
  call the spec didn't ask for).
- **A14 continuous scroll** (`06bfb4d`) — a third reading mode (page → width → scroll);
  a `LazyColumn` reusing the shared LRU cache, scroll-aware turns through the same one
  navigation entry point, persisted like A10. **The reading arc is complete.**

### Text charts — T19 and family (web-core lane)
- **T19** (`9058aa9`) — a member types lyrics/chords/sections in a tiny chart dialect;
  the server renders a PDF into the song's pool as a `generated` file, after which
  everything downstream (view/annotate/my-files/bake/Stage) works unchanged. **Verified
  the whole loop live**: created a chart via the UI (em-dash + bold + chords extract
  correctly, no leaked `**`), then **baked a text-chart setlist and confirmed the baked
  raster's pixels show the rendered chart.** Renderer landed as a fresh package rather
  than the spec'd "move" (the seed's renderer had diverged) — deferral **ratified**,
  filed as T24 (attended). The dropped preview pane → **T25**; the missing UI caveat →
  folded into the T22 fix-forward.
- **T25** (`a3a93da`) — the deferred two-pane preview: a no-persistence
  `…/text-charts:preview` endpoint + a rendered-PDF pane in the editor. **The T19 family
  is now code-complete** (T24 attended-pending).

### Encore/bench — T23, the full raise→rule→implement loop in one session
- The lane **raised a contract-shape question** (`9337a22`) instead of guessing: the
  spec said add an `onCall` item field to proto, but there is no setlist-item proto
  message. **I ruled option A** (`996086b`): proto gains only the bundle field
  `on_call = 8` on `BakedSong`; the item flag rides the Go/TS mirror like `Notes`
  (minting a proper `SetlistItem` message is P203's contract-reshape, not a feature
  task). Coordinated field numbers (T26's `title` → 9).
- **T23** (`59ba8e2`) — implemented exactly to the ruling: bench items bake last,
  jumpable, outside the running numbering; duplicate copies the flag; the Kotlin
  loader's unknown-field tolerance **proven, not cited** (a committed test). Verified
  live (benched a position-0 item → baked last with `onCall:true`; main songs omit the
  field, so old readers are unaffected) and by Studio pixels. A CI e2e flake (a racing
  add-loop, not product) fixed forward (`c8fb34b`).

### Housekeeping
- **B08 filed** (`72087e2`) — a lane flagged a bake test as flaky in chat; I verified
  it's a **real (rare) rev-claim race in `baker.go`** (the publish `rename` has no
  IsExist handling against a concurrently-published rev) and specced the fix. Meta-note
  to the lanes: a suspected-real bug belongs in the log or a task, not just chat.

---

## Decisions made (recorded in specs/reviews.md)

| Decision | Call |
|---|---|
| CFG01 format + precedence | INI (`ini.v1`); defaults < file < env < flags; generated example + byte-equality test; ADR 0004; no SMTP |
| T23 `on_call` proto placement | Option A — bundle field only; item flag is mirror-layer like Notes; the `SetlistItem` message is P203's reshape |
| T19 renderer "move" | Ratified the land-as-fresh-package deviation; convergence deferred to T24 (attended, regenerates pixel-verified artifacts) |
| Reading-ergonomics proposal | Validated; A13 (defect) first, then A15 → A14; two-up "automatic not a mode" stands; encore/bench split to T23 |
| A14 scroll persistence | Global Stage pref (A10 pattern), not per-file (legacy keying is more model than needed) |
| Field-report triage | Misplaced annotations = stale pre-fix data (not a bug); song order = real T22 bug |

## Troubles & incidents (all resolved)

1. **Gate discipline on small tasks** — A13 and CFG01 both landed with no verdict *and*
   no authorization citation (same day the citation habit was re-emphasized). Work was
   flawless both times, so post-hoc approvals — but recorded plainly: the rule has no
   XS exception. By session's end the lanes were citing consistently again (T19, T23,
   T25 all cited; T23 even *raised a question and held* rather than guess).
2. **A15 landed before its evidence** — the code-half verdict required a screenshot
   pre-land; it landed without one. Not a revert (code approved + CI-gated); the shot
   was owed as a fix-forward and delivered folded into A14's evidence run. A15 then
   closed on pixels.
3. **T23 e2e flake** — a green landing went red on CI when a racing add-loop in the new
   spec lost to a slower runner; diagnosed and de-flaked (test-only).
4. **Bake rev-claim race** (B08) — surfaced as an intermittent CI flake on T19's and
   T25's first `go`-job runs; verified as a real baker.go defect, not test noise, and
   filed rather than left in a chat message.
5. **Credential in the git remote** — re-flagged: with no `gh` in this environment,
   querying CI via the REST API means the embedded PAT echoes into tool output every
   time. Rotation + a credential helper remains owed (long-standing, on Vincent).

## Current state

**The product is feature-rich end to end.** Compose → bake → offer → download → import
→ perform works in-app on Android + iOS-sim (unchanged since the weekend); on top of
that, performers now get metadata, hardware/volume/pedal turns, night mode, count-in,
facing pages, a song drawer, and continuous scroll on Stage; and authors get text
charts with a live preview, setlist duplication, encore/bench songs, deterministic
lists, per-member bakes, admin password reset, LAN discovery, and a config file.

**Open, unblocked:** T26 (song titles in the bundle — the drawer showed "Song N"),
the T23 A-track drawer grouping, B06 app-half browse, B08 (bake race), OPS01, P20x.
**Attended-only:** T15 (Viewer split), T17 (toolbar redesign, e2e-first), T24
(chartpdf/mkcharts convergence), the B07 device screenshot pair. **On Vincent:** the
tablet stylus spike (A07), Mac + Apple ID (IOS03), git-remote credential rotation.

*Everything above is one verdict per landing in `reviews.md`; the always-current queue
is `docs/tasks/README.md` § "Queue state".*
