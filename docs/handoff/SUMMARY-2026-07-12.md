# Summary — Saturday 2026-07-11 (late) → Sunday 2026-07-12 (architect/reviewer)

*Picks up where [`SUMMARY-2026-07-11.md`](SUMMARY-2026-07-11.md) ends (queue emptied;
P203 re-affirmed). Every claim is backed by a verdict in [`reviews.md`](reviews.md);
every landing patch-identity-checked and CI-green (with one same-day red owned + fixed).*

## TL;DR

**Two arcs, driven almost entirely by VLL field use of his own box.** The
**editor-reachability arc** (a T27 full-bleed regression had clipped song
management off-screen): the metadata half, then the whole rest — add PDF/text
files, delete files, delete the song — restored into the Details panel, killing
the "Playwright-reachable but human-unreachable" bug class at its substrate. Then
the **chart-authoring arc T37→T39**: lyrics import (SSRF-guarded best-effort
azlyrics/URL fetch + paste fallback), opt-in verse/chorus auto-labeling, and a
dependency-free syntax-highlighted "Lyrics & chords" editor. Plus: the wet-ink
alpha-stacking fix (faster than before), the mobile lane's whole queue closed
(T26/T23/B06), a text chart seeded into the demo, the README refreshed, P201
re-specced, and two design overrides where VLL's direct word corrected a
confirmation I'd taken through the question tool.

## The editor-reachability arc (VLL field blockers)

- **Song metadata reachable** (`5a457a3`): the T27 full-bleed layout had clipped
  SongEditor's `<Details>` off-screen — no way to edit key/tempo/artist. Wired the
  existing `<Metadata>` into the Viewer's Details panel. Its spec also caught
  `flows.spec` silently editing the *clipped* form (the class again).
- **Files + Delete song reachable** (`fa08727`, T36 — a HIGH blocker): the rest
  of the clipped surface — upload PDF/image, ＋ New text chart, delete file,
  delete song — moved into the Details panel (four sections, scrolls, Danger
  zone last). The clipped `<Details>` was **deleted, not bypassed** — removing the
  substrate that let this class recur three times. 24 specs got an
  open-the-panel mechanic; **zero `expect()` lines changed** (freeze verified
  mechanically); an `elementFromPoint` probe guards the panel tail.

## The chart-authoring arc (T37 → T38 → T39)

- **T37 lyrics import** (`5966b78`): VLL **overrode** the earlier paste-only
  decline ("azlyrics is a must" — his ToS/copyright call for a private self-hosted
  tool). Built to spec: `POST /lyrics-import` with an **SSRF guard I read
  line-by-line** — `safeDialContext` resolves + rejects any blocked IP then dials
  the *validated* IP (no DNS-rebinding TOCTOU), covers redirect hops, blocks
  loopback/private/link-local/ULA/metadata; honest GET, no evasion (a Cloudflare
  wall → `blocked`, never a 500); parsers off-network against committed fixtures;
  paste fallback never dead-ends. The exhaustive `isBlockedIP` table (boundary
  cases included) was the #1 acceptance criterion.
- **T38 auto-sections** (`2c213d6`): opt-in `## Verse N`/`## Chorus` labeling,
  **toggle default OFF** (VLL's reconciled call), a pure client-side
  `detectSections` fully separate from the minimal normalizer (untouched);
  chorus by verbatim repeat; title-safe by construction.
- **T39 "Lyrics & chords" editor** (`febb592`): dialect syntax highlighting via a
  **dependency-free** monospace overlay (custom over CodeMirror — ruled
  proportionate for a ~4-token dialect); **preview stays on-demand** (T25 upheld,
  no auto-render on type — probe-verified); relabeled "Lyrics & chords" (VLL:
  "chart editor is misleading"). The TS chord regex is character-identical to
  chart.go's — the highlight matches what the PDF renders.

## Other landings

- **T35 wet alpha-stacking** (`21698ac`): VLL's slow-freehand dark bands — a
  wet-only multi-pass-alpha bug (dry render was always fine). Fixed with
  uniform-alpha compositing (opaque compose → one blit at opacity, mirroring ink's
  Box precedent) + a capture-time point diet. My spec under-counted the cost; I
  ruled **option (b)** (bbox-restricted ops) for the tablet headroom, and it
  landed **faster than pre-fix** (the bbox bound also removed the old
  full-cache-per-frame blit).
- **Mobile queue closed:** **T26 app half + T23 drawer** (`309f06f`, baked titles
  + bench grouping) and **B06 app half** (`4e0c024`, LAN Connect-screen mDNS
  browse — no new I15 seam; iOS NWBrowser deferral ratified). The unattended
  mobile queue is now empty.
- **B10 demo text chart** (`0c760ed`): the seed now renders a real text chart
  (original safe lyrics) into The Open Road's pool, so the demo shows the T19
  type — honest note that the shared bake keeps the annotated lead sheet (one
  file per song). **My gofmt slip:** the B10 edit realigned a struct literal and
  I misread `gofmt -l` (it exits 0 even when it lists a dirty file, so my
  `&& echo CLEAN` lied) → red on a later go job → fix-forward `502a569`. Owned;
  memory updated (never chain `gofmt -l` with `&& echo`).
- **README refreshed** (`ab63670`): CI/iOS badges, Go/Node/Kotlin shields, current
  screenshots, a real Docker "install it" section, honest packaging status (no
  registry image / releases / store APK yet; no LICENSE — VLL's call).
- **P201 refreshed** (`079acac`) against current main (banner placement for the
  fullscreen editor; R10 matrix extended to facing-pages + scroll mode). **P203
  DECIDED:** re-affirm mirrors (VLL). USER-JOURNEY + mobile handoff refreshes
  approved post-hoc.

## Process notes (mine to own)

- **Two design overrides.** VLL's direct calls to the lane diverged from
  confirmations I'd taken via the question tool (T38 default, T39 intent). The
  lane's reconcile-before-build instinct was the safety net; the human owner's
  latest word wins, and I re-specced both. A confirmation is only as good as its
  currency.
- **One retracted finding** (OPS01 `core/internal/gen`) and **one owned gofmt
  miss** (B10) — both caught quickly, both recorded. The gate is adversarial in
  both directions, which is the point.

## Current state

The chart-authoring and editor-reachability arcs are complete; the product now
composes songs (files, charts, lyrics import, highlighted editing) entirely
inside the fullscreen editor. **Open:** P201 (rehearsal live mode — ready when
VLL says go). **Attended (VLL):** OPS01 bring-up + release APK, the device pass
(T27 feel + viewport caveats + live mDNS), A07, T24, B07 shots. **Decisions
(VLL):** LICENSE, credential rotation. **Deferred:** P204 (pressure). Every
landing CI-green.

*Verdicts: `reviews.md`. Queue: `docs/tasks/README.md` § "Queue state".*
