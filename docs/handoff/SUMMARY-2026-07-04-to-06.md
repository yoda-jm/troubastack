# Weekend summary — Saturday 2026-07-04 → Monday 2026-07-06 (architect/reviewer)

*Written by the Architect/Reviewer session for Vincent's review. Every claim below is
backed by a verdict entry in [`reviews.md`](reviews.md) (~30 entries this weekend) and
every landing was independently re-verified — builds/tests re-run fresh, artifacts
dissected, screenshots checked by pixels. Commits are as-landed on `main` (linear, all
CI-green unless noted).*

## TL;DR

**The product loop closed this weekend.** On Friday night the bake was a stub and iOS
didn't exist. As of Monday night: **compose → bake → offer → download → import →
perform works end to end, in-app, on Android and iOS-simulator**, with the shipped demo
bundle produced by the real pipeline and containing an original song. ~25 landings, 5
new invariant-✅ flips, zero regressions shipped, and every review finding either fixed
or explicitly accepted.

---

## Saturday 07-04 — iOS from zero to simulator-proven

- **LRU portability fix** (`b146356`) — the IOS01 spike caught a JVM-only
  `LinkedHashMap` in *shipped* A04 code; fixed + landed first as its own PR.
- **IOS01** (`8e53e42`) — iOS targets enabled; Storage (NSFileManager + Keychain +
  zip reader) and WebViewHost (WKWebView) actuals filled. Review demanded the size-cap
  guard be made portable + JVM-tested (done pre-land). The portable zip reader
  (EOCD→central-dir, zip-slip + size-cap guards, `expect rawInflate`) lives in
  commonMain and is unit-tested.
- **B01** (`00a7da5`) — the real I8: `web/bake` renders annotation overlays through the
  SAME `@troubastack/ink` code studio uses, guarded by the **golden pixel-parity test**
  (bake vs. headless-Chromium dry path, ≥99% within Δ≤3, transparency ≥99.9%). Review
  additionally ran the REAL Kotlin loader against bake output — zero issues.
- **T13** (`66fcb19`) — the CI-only RO/RW 27px layout shift root-caused (pill wrap
  under CI's fallback font) and fixed with the reserve-space pattern; quarantine
  removed, e2e hard-gates 56/56 again.
- **IOS02** (`e786418` + fixes) — the marathon: **8 dispatched macOS runs, 5 reviewed
  fix-forwards** to a green simulator proof. The ladder: arm64-vs-x86_64 link failure →
  device/runtime mismatch (`[-1]` picked iPhone 6s Plus for iOS 26.5) → **run #4 "green"
  but screenshots showed the springboard** (the app crashed AFTER writing the smoke
  marker — caught only by pixel review) → hard liveness asserts + crash diagnostics →
  console capture named it: **`CADisableMinimumFrameDurationOnPhone` missing from the
  generated Info.plist** (Compose Multiplatform's hard iOS requirement) → plist fix →
  console-relay kill was then killing its own app → plain launch → **run #8 green with
  real Wonderwall Stage pixels**. Every red made the workflow permanently smarter.
- **T16** (`96ae26c`, prior session) — seed PDF em-dash mojibake fixed at the source.
- Docs: handoff files for session continuity; T14/T15 steered.

## Sunday 07-05 — B02 closes bake→download; honest reverts

- **T14 closed WITHOUT landing** — the panelize approach was built, measured (~10px of
  the 150px goal), and reverted. Superseded by T17.
- **T17 GO'd, then attempt-logged** — the disclosure-as-specced was built, measured,
  and **reverted because it broke zero-shift** (exactly the failure class my decision
  said an e2e gate must catch — constraint validated the hard way). T17 is now
  attended-only with a redesign brief in its attempt log.
- **B02 part 1** (`869d900`) — core bake orchestration: Baker (setlist → poppler
  rasters → B01 worker overlays → canonical bundle.json → deterministic .tstage),
  admin-gated endpoints, proto metadata fields (notes/key/tempo ride the manifest).
  **Review ran the whole loop live**: seed → bake via endpoint (11s, 3 songs) →
  download → all hashes verified → the real Kotlin loader accepted it → re-bake bumped
  rev. Two findings filed as B04.
- **B02 part 2** (`552c516`) — Studio Bake card + e2e spec. Android loop-close deferred
  honestly to an attended run.
- **IOS03 prep runbook** (`d953e51`) — device/TestFlight/Store tiers, signing-out-of-
  repo discipline; blocked on Mac + Apple credentials. Its screen-sleep finding became
  **IOS04** (`5946874`, same day): Stage now holds `idleTimerDisabled` on iOS.
- **B03 server slice** (`97052eb`) — concerts manifest reshaped to the proto
  `AvailableConcert` canonical JSON the app parses verbatim.

## Monday 07-06 — distribution, robustness, original content

- **B02 loop-close evidence** (`c676716`) — the mobile lane baked for real, injected on
  the emulator, and performed it; **B02 stamped CLOSED on screenshot evidence**.
- **B03 app half** (`5e54c6f` + `7f60bd0`) — `UpdatesManager` (offer/freeze/pin diff —
  10-case unit matrix; apply via A05's atomic import), **EncryptedSharedPreferences
  secrets hardening** (A05's caveat paid before the first token), ktor transport +
  Connect screen + offer chips, all e2e'd on the emulator. **I13 explicit tier → ✅.**
- **B04** (`d085b63`) — bake concurrency race + partial-publish window closed (atomic
  `os.Mkdir` rev claim + stage-then-rename publish); verified 3× under `-race`.
- **T18** (`c5de23b`) — the duplicate Go ConcertBundle mirror deleted; `make fixtures`
  regenerated byte-identical (proving no drift had occurred).
- **B05** (`4751bb4`) — first BLOCKED honestly (3 spec conflicts, incl. a reviewer
  error: the spec named the Vocals part, the pipeline bakes the default Score part);
  decision (b) chosen → **the shipped demo is now the real-baked 4-song concert**, with
  the em-dash correct in the distributed bytes.
- **"The Open Road"** (`b56ffb9`) — an original song (committed original chart) wired
  into the seed with three purposeful annotation layers; the demo now showcases real
  sheet content through the whole pipeline.
- **Mobile follow-ups** (propose→GO→land: `ed11ec6`, `09937c9`) — iOS size-gate before
  read, `rawInflate` fail-closed on BOTH platforms (with the zlib double-call subtlety
  handled), `jsQuote` and a generic `LruCache` hoisted to commonMain with tests.
  **Every parked review note from the whole arc is now fixed or explicitly accepted.**
- **Shared App()/nav proposal** — decision: **defer** until an iOS transport provides a
  second real consumer (anti-speculative-generality; trigger recorded).
- **Architecture audit** (`5602ff5`, `3852267`, `1669b56`) — I8/I11/I12/I13/I15 status
  tags caught up with reality; **one constitutional amendment**: I8's rule text still
  claimed "headless studio / pixel-identical by construction" — amended to the built
  mechanism (same code + parity test). Tasks README gained a maintained "Queue state".
- **Tablet screenshots** (`9e79a9c`) — produced on a new **Pixel Tablet AVD**
  (1600×2560 @276dpi, portrait); The Open Road with its baked layers on the stage-time
  form factor is now in the README.

---

## Decisions made (all recorded in specs/reviews.md)

| Decision | Call |
|---|---|
| T17 style-bar direction | GO on disclosure; after the measured regression → attended redesign, e2e gate FIRST |
| IOS02 acceptance | land-then-dispatch (GitHub can't dispatch branch-only workflows); closed only on artifact pixels |
| B02 metadata | setlist overrides ride the manifest, never pixels (spec'd; proven end to end) |
| B05 demo scope | option (b): demo = the real-baked multi-song concert (reviewer's "Vocals" spec error acknowledged) |
| Shared App()/nav hoist | DEFER until an iOS transport exists (trigger recorded) |
| LAN discovery (B06) | spec'd: mDNS prefill only, never trust; `_troubacore._tcp`; opt-out env |
| MIDI pedals | out of scope (BT pedals are keyboards → A09); revisit only if the real hardware is MIDI-only |

## Troubles & incidents (all resolved)

1. **IOS02's 8-run ladder** (above) — the big one; also exposed that a green CI run can
   lie (marker passed, app dead) — pixel review caught it.
2. **T17 regression** — caught by measurement before landing; validated the e2e-first rule.
3. **B05 spec conflicts** — including a reviewer error; the blocked-report discipline worked.
4. **Port collision** — a stale agent server owned :8093 and silently answered my B02
   E2E with pre-B02 404s (caught via a wrong `createdAt`); moral: always verify you're
   talking to YOUR server.
5. **Emulator decode lag** — paging faster than the async decode shows a blank page
   under swiftshader; cosmetic, on-hardware fine (noted for future screenshot work).
6. **Process drift, corrected**: early landings skipped the review gate silently
   (B01, T13); after two notes the lanes adopted **authorization lines in commit
   messages** ("landed per steer + VLL") and the **propose→GO→implement** pattern for
   unspecced work. One Monday landing (Open Road) missed the line — reminded once.

## Current state

- **Done:** T01–T13, T16, T18 · A01–A06 · B01–B05 · IOS01, IOS02, IOS04 · demo +
  screenshots current. All CI-green, all reviewed.
- **Open, unblocked:** B06 (mDNS discovery) · A08 (Stage metadata strip) · A09
  (pedal/volume page turns) · A10 (night mode) · OPS01 · P201–P203.
- **Attended-only:** T15 (Viewer split), T17 (toolbar redesign — read its attempt log).
- **Blocked on you:** ① tablet stylus spike (decides A07 build-or-close), ② Mac +
  Apple ID (IOS03 impl), ③ **rotate the credential in the git remote URL**
  (long-flagged). Open product calls: ④ widen bake from admin-only to members (the I11
  rule permits it)? ⑤ promote P203 (proto codegen) now that the loop is closed — I1 is
  the biggest remaining rule-vs-reality gap.
