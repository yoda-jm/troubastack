__pycache__/
# ad-hoc comparison probes: not produced by build.py or sheet.py
dist/_*.png
dist/_*.json
# generated by tools/make-placement.py; embeds two rasters of the artwork
dist/placement.html
# every raster in dist/ is an OUTPUT of build.py --png and sheet.py
dist/*.png
# the ChatGPT exploration plates the design was measured against. Kept out of
# the repo: they are external sources, not our artefacts. Drop them back into
# reference/ if a measurement has to be redone.
reference/
