{
  "//": "Node worker. @troubastack/ink is compiled from source here (see paths), so bake needs the DOM lib to typecheck the Canvas2D types ink's API exposes — DOM is a compile-time type surface only, not a browser runtime. esbuild does the real emit (see esbuild.mjs); tsc runs --noEmit as the typecheck gate. test/browser-entry.ts is the parity page's browser bundle — type-only here, esbuild bundles it for Chromium. Shared wire types come from web/proto-gen (I1).",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM"],
    "strict": true,
    "noEmit": true,
    "verbatimModuleSyntax": true,
    "skipLibCheck": true,
    "types": ["node"],
    "paths": {
      "@troubastack/ink": ["../ink/src/index.ts"]
    }
  },
  "include": ["src", "test/browser-entry.ts"]
}
