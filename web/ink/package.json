{
  "name": "@troubastack/ink",
  "version": "0.0.0",
  "private": true,
  "description": "THE single TroubaStack stroke renderer (I8). Consumed by studio (browser) and bake (Node). Native overlay mirrors it under a parity test; no third copy.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "//deps": "perfect-freehand is the intended geometry lib for buildStrokeGeometry — NOT installed yet (scaffold, no network).",
  "dependencies": {}
}
