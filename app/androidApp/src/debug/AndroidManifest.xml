<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG-ONLY overlay: permit cleartext HTTP so dev builds can reach a LAN/localhost core
     (e.g. http://10.0.2.2:8080 from the emulator). Release builds never get this config. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
